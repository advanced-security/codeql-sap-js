name: QL Unit Tests (Linux)

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  create-unit-test-matrix:
    name: Create CodeQL Unit Test Matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.export-unit-test-matrix.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install QLT
        id: install-qlt
        uses: advanced-security/codeql-development-toolkit/.github/actions/install-qlt@177984f7fc369b131c844b3a50d8d1e9a8e5223b # main
        with:
          qlt-version: 'latest'
          add-to-path: true

      - name: Export unit test matrix
        id: export-unit-test-matrix
        run: |
          qlt test run get-matrix --os-version ubuntu-latest

  run-test-suites:
    name: Run Unit Tests
    needs: create-unit-test-matrix

    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.create-unit-test-matrix.outputs.matrix) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install QLT
        id: install-qlt
        uses: advanced-security/codeql-development-toolkit/.github/actions/install-qlt@177984f7fc369b131c844b3a50d8d1e9a8e5223b # main
        with:
          qlt-version: 'latest'
          add-to-path: true

      - name: Cache CodeQL CLI
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.qlt/packages
          key: ${{ runner.os }}-codeql-cli-${{ hashFiles('qlt.conf.json') }}

      - name: Install CodeQL
        id: install-codeql
        shell: bash
        run: |
          echo "Installing CodeQL"
          qlt codeql run install

          codeql_path="$(find "$HOME/.qlt/packages" -type f -path '*/codeql/codeql' -perm -u+x -print -quit)"
          if [[ -z "$codeql_path" ]]; then
            echo "::error::CodeQL executable not found in $HOME/.qlt/packages"
            exit 1
          fi

          codeql_home="$(dirname "$codeql_path")"
          echo "QLT_CODEQL_HOME=$codeql_home" >> "$GITHUB_ENV"
          echo "QLT_CODEQL_PATH=$codeql_path" >> "$GITHUB_ENV"
          echo "-----------------------------"
          echo "CodeQL Home: $codeql_home"
          echo "CodeQL Binary: $codeql_path"

      - name: Verify Versions of Tooling
        shell: bash
        run: |
          echo -e "Checking CodeQL Version:"
          $QLT_CODEQL_PATH --version

          echo -e "Checking QLT Version:"
          echo "QLT Home: ${{ steps.install-qlt.outputs.qlt-home }}"
          qlt version
      
      - name: CodeQL Compilation Cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/codeql-compilation-cache
          key: ${{ runner.os }}-codeql-compilation-${{ github.workflow }}-${{ matrix.codeql_cli }}-${{ matrix.codeql_standard_library_ident }}-${{ hashFiles('javascript/**/*.ql', 'javascript/**/*.qll', 'javascript/**/qlpack.yml', 'javascript/**/codeql-pack.lock.yml') }}
          restore-keys: |
            ${{ runner.os }}-codeql-compilation-${{ github.workflow }}-${{ matrix.codeql_cli }}-${{ matrix.codeql_standard_library_ident }}-
            ${{ runner.os }}-codeql-compilation-${{ github.workflow }}-
            ${{ runner.os }}-codeql-compilation-

      - name: Install QL Packs
        shell: bash
        run: |
          qlt query run install-packs

      - name: Setup Node.js for CDS compilation
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'extractors/cds/tools/package-lock.json'

      - name: Verify Node.js and npm tools
        run: |
          echo "Node.js version: $(node --version)"
          echo "npm version: $(npm --version)"
          echo "npx version: $(npx --version)"
          # Verify npx can access @sap/cds-dk without installing globally
          echo "Testing npx access to @sap/cds-dk..."
          npx --yes --package @sap/cds-dk@latest cds --version || echo "CDS will be installed per-project as needed"

      # Compile .cds files to .cds.json files using the dedicated test script
      - name: Compile CAP CDS files
        run: |
          # Use the dedicated CDS compilation script that includes proper version resolution
          # This script follows the same logic as the CDS extractor's resolveCdsVersions function
          ./extractors/cds/tools/workflow/cds-compilation-for-actions.sh

      - name: Run test suites
        id: run-test-suites
        env:
          RUNNER_OS: ${{ runner.os }}
          CODEQL_CLI: ${{ matrix.codeql_cli }}
          CODEQL_STDLIB: ${{ matrix.codeql_standard_library }}
          CODEQL_STDLIB_IDENT: ${{matrix.codeql_standard_library_ident}}
          RUNNER_TMP: ${{ runner.temp }}
          LGTM_INDEX_XML_MODE: all
          LGTM_INDEX_FILETYPES: ".json:JSON\n.cds:JSON"

        shell: bash
        run: >
          qlt test run execute-unit-tests
          --codeql-args "--threads=0 --strict-test-discovery --compilation-cache=${{ runner.temp }}/codeql-compilation-cache"
          --num-threads 2
          --language javascript
          --runner-os $RUNNER_OS
          --work-dir $RUNNER_TMP

      - name: Upload test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-results-${{ runner.os }}-${{ matrix.codeql_cli }}-${{ matrix.codeql_standard_library_ident }}
          path: |
            ${{ runner.temp }}/test_report_${{ runner.os }}_${{ matrix.codeql_cli }}_${{ matrix.codeql_standard_library_ident }}_slice_*.json
          if-no-files-found: error

  validate-test-results:
    name: Validate test results
    needs: [run-test-suites]
    runs-on: ubuntu-latest
    steps:

      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install QLT
        id: install-qlt
        uses: advanced-security/codeql-development-toolkit/.github/actions/install-qlt@177984f7fc369b131c844b3a50d8d1e9a8e5223b # main
        with:
          qlt-version: 'latest'
          add-to-path: true


      - name: Collect test results
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1

      - name: Validate test results
        run: |
          qlt test run validate-unit-tests --pretty-print --results-directory . >> $GITHUB_STEP_SUMMARY
          qlt test run validate-unit-tests --results-directory .
